{{> head }}
{{> navbar }}

  <div class="jumbotron jumbotron-fluid">
    <div class="container">
      <h1 class="display-4">{{ nombre }}</h1>
      <p class="lead">{{ capitalizar nombre }}</p>
    </div>
  </div>

{{> footer}}